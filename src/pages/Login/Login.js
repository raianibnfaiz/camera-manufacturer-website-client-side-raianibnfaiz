import React from 'react';

const Login = () => {
    return (
        <div>
            <h1>Login</h1>
            <button class="btn btn-primary">Button</button>
        </div>
    );
};

export default Login;